# CLAUDE.md — 《클라이머 키우기》 작업 규칙

이 파일은 기획서가 아니라 **모든 작업에서 지켜야 할 강제 규칙**이다.
새 기능을 만들기 전에 반드시 읽고, 규칙과 충돌하면 규칙이 이긴다.

## 1. 게임 정체성 (변경 금지)
- 모바일 우선 **2D 도트 방치형 클라이밍 육성 RPG**. 배경은 부산, 시작 암장은 콘텐츠 데이터로 지정.
- 등반은 **포켓몬 전투식 턴제 선택**이다. 상황 → 베타 선택 → 연출 → 결과.
- 접속하지 않아도 일정은 진행된다. 접속하면 **더 빠르고 더 좋은 결과**를 얻는다.
- 실패는 처벌이 아니라 **귀여운 사건**이다. 문구를 무섭게 쓰지 마라.
- 유저가 하는 일은 단순하고, 내부 성장 구조는 깊다.

## 2. 절대 금지
- ❌ **타이밍 입력 금지.** 연타, 게이지 멈추기, 반응속도, QTE, 드래그 정밀 조작 전부 금지.
- ❌ 게임 엔진(Phaser/Unity/무거운 캔버스 루프) 도입 금지. React 컴포넌트 + CSS/SVG만 사용.
- ❌ 외부 이미지 URL, 저작권 불명 에셋, 유료 에셋 금지. 저장소 내부에서 생성한 것만.
- ❌ 형광색·과한 네온·유리 효과·큰 그라데이션 금지.
- ❌ UI에만 존재하는 가짜 스킬/가짜 버튼 금지. 미구현은 "준비 중"으로 명시.
- ❌ 서비스 롤 키, 시크릿, 환경변수 값을 코드에 넣지 마라.

## 3. 코드 경계 (가장 중요)
- **게임 판정은 UI에 넣지 마라.** 성공률·보상·피로 계산은 전부 `src/game/*.ts` 순수 함수다.
  - `src/game/climb.ts` — 등반 한 동작 판정
  - `src/game/progress.ts` — 일정 진행 / 오프라인 계산
  - `src/game/character.ts` — 파생 스탯, 관절 단계, 경고
- **컴포넌트는 store를 읽고 액션을 호출할 뿐**이다. `Math.random()`을 UI에서 부르지 마라.
- 난수는 `src/game/rng.ts`의 주입 가능한 RNG만 쓴다. 테스트는 시드 고정으로 재현한다.
- **밸런스 숫자는 `src/game/balance.ts` 한 곳에만** 둔다. 다른 파일에 매직넘버 금지.
- **콘텐츠는 코드가 아니다.** 암장·문제·활동·스킬·NPC는 `src/content/*.ts` 데이터로만 추가한다.
  새 암장/문제를 추가하려고 `src/game`이나 `src/ui`를 고쳐야 한다면 설계가 잘못된 것이다.
- 저장은 `src/store/storage.ts`의 어댑터 인터페이스를 통해서만 한다. `localStorage`를 직접 부르지 마라.
- **온보딩 임시 상태와 본 세이브는 분리한다.** 마지막 확인을 누르기 전에는 기존 세이브를 건드리지 마라
  (`saveDraft`/`loadDraft`/`clearDraft` vs `save`/`load`).

## 4. 서버 권한 원칙
- 지금은 로컬 데모 모드(게스트 클라이머)다. **클라이언트 계산은 임시**다.
- 시간은 `src/game/clock.ts`의 `now()`만 사용한다. 나중에 서버 시간으로 교체할 지점이다.
- 랭킹·보상·성공 판정은 장기적으로 서버에서 재계산한다. 클라이언트가 결과 수치를 그대로
  제출하는 코드를 새로 만들지 마라. Supabase 어댑터는 `// SERVER-AUTHORITY` 주석으로 표시한다.

## 4-1. 캐릭터 생성 공정성 (절대 규칙)
- **모든 캐릭터의 시작 능력치 총합은 동일하다** (`BALANCE.creation.total`).
- **성별은 능력치·체형·키에 전혀 영향을 주지 않는다.** 외형 후보와 문구에만 쓴다.
  구조로 보장돼 있다 — `generateCharacter()`에서 성별/나이는 rng를 소비하지 않는다.
- **나이도 능력치에 영향을 주지 않는다.** 특정 나이를 골라야 유리한 구조를 만들지 마라.
- 주특기는 능력치를 **재분배**할 뿐 총합을 늘리지 않는다.
- 체형은 능력치를 바꾸지 않는다. 등반 판정(먼 홀드/압축)에만 연결된다.
- 키는 **표시용**이다. reach와 중복으로 성공률에 적용하지 마라.
- 세 지점(서면·남천·부산대)은 능력치·보상이 **완전히 동일**하다. 구분은 시각 요소뿐이다.
- 랜덤 생성·능력치 배분은 전부 `src/game/characterGen.ts`의 순수 함수다.
  **React 컴포넌트 안에서 굴리지 마라.** 시드를 저장해 언제나 같은 캐릭터가 복원돼야 한다.

## 5. 한국어 / 텍스트
- 모든 UI·대사·경고는 자연스러운 한국어. 실제 클라이머가 공감할 표현을 쓴다.
- 경고는 겁주지 말고 귀엽고 명확하게. 예: "손가락이 조용히 파업을 준비하고 있어요."
- 확률은 기본적으로 숫자 대신 등급(매우 유리/해볼 만함/위험/무모함/알 수 없음)으로 표시한다.
  정확한 숫자는 루트파인딩 스킬·NPC 친밀도로 해금될 때만 보여준다.

## 6. 에셋
- 도트만. 벡터 곡선 금지. 확대 시 `image-rendering: pixelated`.
- 캐릭터는 **몸통 픽셀맵 1개 + 팔다리 좌표 포즈**로 만든다. 포즈마다 새 스프라이트 시트를 만들지 마라.
- 머리 형태 6종도 좌표 테이블(`HAIRS`)이다. 외형 확장은 **팔레트 색 추가**로 한다.
- 홀드는 기본 형태 6~8개 + 색/크기/각도 조합. 문제마다 새 이미지를 만들지 마라.
- 새 에셋을 추가하면 `ASSET_MANIFEST.md`에 반드시 기록한다.

### 캐릭터 크기 (중요)
- `<Sprite size="tiny|small|medium|action" />` **4단계만** 쓴다. **px를 직접 넘기지 마라.**
  - `tiny` 32 (목록·NPC) / `small` 48 (홈·리포트) / `medium` 64 (생성 미리보기·성장) / `action` 32 (등반)
- 값은 전부 **16의 정수배**다. 픽셀 정렬이 깨지므로 다른 숫자를 추가하지 마라.
- 15개 포즈는 전부 같은 16×22 그리드와 같은 발 기준선을 쓴다(바닥에서 뜨지 않게).
- 캐릭터를 눌러야 하면 `<SpriteButton>`을 쓴다 — 시각 크기와 무관하게 터치 영역 44px을 보장한다.

## 7. 코드 품질
- TypeScript strict. `any`로 타입 오류를 덮지 마라. 오류를 삼키지 마라.
- 거대 컴포넌트 금지. 화면 1개 = 파일 1개, 판정 로직은 게임 레이어로 뺀다.
- 임시 구현에는 `// TEMP:` 주석과 교체 조건을 남긴다.
- 기존 사용자 파일과 저장 데이터를 보존한다. 세이브 구조를 바꾸면 `SAVE_VERSION`을 올리고 마이그레이션을 쓴다.

## 8. 명령어
```
npm run dev        # 개발 서버 (http://localhost:8950)
npm run test       # vitest 단위 테스트
npm run typecheck  # 타입 검사
npm run build      # 타입 검사 + 프로덕션 빌드
```

## 9. 작업 완료 조건
새 작업은 아래를 모두 만족해야 "완료"다.
1. `npm run test` 통과 (게임 로직을 건드렸으면 테스트 추가/갱신)
2. `npm run build` 통과
3. 390×844px에서 가로 스크롤 없음, 하단 탭이 안전영역과 겹치지 않음
4. 새로고침 후 상태가 복원됨
5. `docs/IMPLEMENTATION_PLAN.md` 체크리스트 갱신
6. 기존 핵심 루프(일정 편성 → 시간 진행 → 등반 → 성장)가 여전히 동작
7. **기존 세이브가 손실되지 않는다.** 필드를 추가하면 `SAVE_VERSION`을 올리고
   `migrate.ts`에 기본값 병합을 추가한 뒤 마이그레이션 테스트를 쓴다.

## 10. 기존 기능 보존
기능을 추가할 때 핵심 루프를 깨뜨리지 마라. 리팩터링이 필요하면 테스트를 먼저 통과시킨 뒤에 한다.
관련 없는 파일은 건드리지 않는다.
