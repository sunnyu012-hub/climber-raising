# 에셋 매니페스트

**외부 에셋을 하나도 쓰지 않았다.** 아래는 전부 저장소 안에서 코드로 생성한 자체 플레이스홀더다.
외부 이미지 URL, 유료 에셋, 저작권 불명 이미지 없음. 폰트도 시스템 폰트 폴백만 사용한다.

## 1. 캐릭터 스프라이트
| 항목 | 값 |
|---|---|
| 위치 | `src/ui/Sprite.tsx` |
| 형식 | 인라인 SVG `<rect>` (이미지 파일 없음) |
| 그리드 | 16 × 22 픽셀 |
| 표시 크기 | **4단계 토큰만 사용** — `tiny` 32 / `small` 48 / `medium` 64 / `action` 32 |
| 렌더링 | `shapeRendering="crispEdges"` + CSS `image-rendering: pixelated` |
| 임시 여부 | ✅ 임시 (자체 제작 플레이스홀더) |

**크기 체계 — 중요**
화면마다 임의의 px를 쓰지 않는다. `<Sprite size="small" />` 처럼 토큰만 넘긴다.
네 값 모두 **16(그리드 너비)의 정수배**라 확대해도 픽셀이 뭉개지지 않는다.
| 단계 | px | 쓰는 곳 |
|---|---|---|
| `tiny` | 32 | 목록 · 상태 카드 · NPC 관계 |
| `small` | 48 | 홈 프로필 카드 · 오프라인 리포트 |
| `medium` | 64 | 캐릭터 생성 미리보기 · 성장 화면 |
| `action` | 32 | 등반 장면 — 벽과 홀드가 크게 느껴지도록 제일 작게 |

캐릭터를 눌러야 하면 `<SpriteButton>`을 쓴다. 시각 크기와 무관하게 **44×44px 터치 영역**을 보장한다.

**재사용 방식 — 중요**
포즈마다 스프라이트 시트를 만들지 않는다. 몸통·머리는 고정이고 **팔/다리 사각형 좌표만 교체**한다.
포즈 15종(`idle, lookUp, reach, stepUp, pull, highstep, flag, heel, dyno, wobble, fall, mat, top, tired, rest`)이
전부 `POSES` 테이블의 좌표 배열 한 줄씩으로 정의된다. 새 포즈 = 배열 한 줄 추가.

동작감은 CSS 키프레임 5개로 만든다 (`styles.css`): `breathe`, `shake`, `tumble`, `cheer`, `sparkle`(다시 뽑기).

**머리 형태 6종**도 같은 방식이다 — `HAIRS` 테이블의 사각형 좌표 배열.
`short` / `long` / `bun` / `curly` / `twin` / `cap`. 새 형태 = 배열 한 줄 추가.

**교체 방법**
진짜 픽셀 아티스트 에셋으로 바꿀 때는 `Sprite.tsx`의 렌더 부분만 `<image href="...">`로 교체하고
`POSES` 키는 그대로 유지한다. 게임 로직은 `PoseKey`만 알고 있으므로 다른 파일은 손댈 필요가 없다.

## 2. 팔레트 (외형 커스터마이징)
| 항목 | 값 |
|---|---|
| 위치 | `src/content/appearance.ts` (풀) · `src/ui/Sprite.tsx` (기본값) |
| 구성 | skin / hairColor / hair(형태) / shirt / pants / shoe / chalkbag |

| 파츠 | 종류 수 |
|---|---|
| 피부 | 5 |
| 머리 색 | 8 |
| 머리 형태 | 6 |
| 상의 | 10 |
| 하의 | 8 |
| 암벽화 | 8 |
| 초크백 | 8 |

파츠 이미지를 늘리는 대신 **색 조합**으로 외형을 확장한다. 색 추가 = 배열에 hex 하나.
저장된 `appearance`(+`seed`)로 항상 같은 외형이 복원된다 — 새로고침해도 바뀌지 않는다.
15개 포즈 전부에서 같은 외형이 유지된다.

> 성별로 팔레트를 나누지 않는다. 어떤 성별이든 모든 색이 나올 수 있다.

## 3. 클라이밍 벽 + 홀드
| 항목 | 값 |
|---|---|
| 위치 | `src/ui/Wall.tsx` |
| 형식 | 인라인 SVG, viewBox `0 0 120 160` |
| 홀드 기본 형태 | **6종** — 저그 / 크림프 / 슬로퍼 / 핀치 / 포켓 / 볼륨 |
| 홀드 색 | 6색 팔레트 |
| 배치 | 문제 `id` 해시 기반 결정론적 생성 (같은 문제 = 항상 같은 벽) |
| 배경 구성 | 벽 패널 + 볼트 점 + 안내판 + 탑 홀드 + 매트 + 벤치 |
| 임시 여부 | ✅ 임시 |

**문제를 추가해도 새 이미지가 필요 없다.** 홀드 개수는 `steps.length × 3 + 4`로 자동 계산되고,
벽 배경색은 `wall`(슬랩/수직/오버행/루프)에 따라 자동으로 달라진다.

## 4. UI 그래픽
| 항목 | 값 |
|---|---|
| 위치 | `src/styles.css` |
| 형식 | CSS만 (이미지 0개) |
| 구성 | 픽셀 테두리(2px solid), 계단식 그림자(`3px 3px 0`), `steps()` 트랜지션 |

색 토큰: 크림 `#f6efe2` · 세이지 `#8faa86` · 하늘 `#a8c8dd` · 코랄 `#e8916f` ·
살구 `#f0c088` · 브라운 `#4a3b32` · 네이비 `#2f3a4a`
형광색·네온·유리 효과·큰 그라데이션 없음.

## 4-1. 지점 미리보기 도트
| 항목 | 값 |
|---|---|
| 위치 | `src/ui/OnboardingFlow.tsx` → `GymThumb` |
| 형식 | 인라인 SVG 28×28, 56px 표시 |
| 색 | `Gym.theme` (sign / wall / accent) — 지점마다 다르다 |
| 임시 여부 | ✅ 임시 |

암장 사진이 아니라 색으로 구분되는 도트 썸네일이다. 새 지점은 `theme` 색 3개만 정하면 된다.

## 4-2. 지역·장비 표시 도트
| 항목 | 값 |
|---|---|
| 지점 썸네일 | `src/ui/OnboardingFlow.tsx` `GymThumb` · SVG 28×28 · `Gym.theme` 색 |
| 지도 지점 표시 | `src/ui/menu/WorldScreen.tsx` · 색 사각형 + 방문 표시(✓/?) |
| 장비 색 견본 | `src/ui/GearPanel.tsx` `.slot-swatch` · `EquipmentItem.color` 한 색 |
| 임시 여부 | ✅ 임시 |

장비 아이콘을 그리지 않는다. **색 하나**로 구분한다 — 장비를 늘려도 에셋이 안 늘어난다.

## 5. 아이콘
| 항목 | 값 |
|---|---|
| 형식 | **시스템 이모지** (파일 아님) |
| 사용처 | 하단 탭, 활동, NPC, 관절 상태, 결과 |
| 임시 여부 | ✅ 임시 — 나중에 도트 아이콘으로 교체 예정 |

교체 시 바꿀 곳: `src/ui/TabBar.tsx`의 `ico`, `src/content/activities.ts`의 `icon`,
`src/content/npcs.ts`의 `emoji`, `src/game/character.ts`의 `JOINT_STAGE_EMOJI`.
전부 데이터라 컴포넌트 수정 없이 교체된다.

## 6. 폰트
| 항목 | 값 |
|---|---|
| 지정 | `Galmuri11, DungGeunMo, Apple SD Gothic Neo, Malgun Gothic, system-ui` |
| 번들 | **없음** — 웹폰트 파일을 포함하지 않는다 |

한국어 도트 폰트(갈무리/둥근모)가 설치된 기기에서는 도트 감성이 살고,
없으면 시스템 고딕으로 폴백한다. 웹폰트를 추가하려면 라이선스(OFL 등)를 확인하고
`public/fonts/`에 넣은 뒤 이 문서에 출처를 기록할 것.

## 7. PWA 아이콘
| 항목 | 값 |
|---|---|
| 위치 | `public/manifest.webmanifest` |
| 상태 | `icons: []` — **아직 없음** |

홈 화면 설치 아이콘(192/512 PNG)은 미제작이다. 정식 배포 전에 도트 아이콘을 만들어
`public/icons/`에 넣고 매니페스트와 이 문서를 갱신할 것.

## 8. 저작권
| 항목 | 출처 |
|---|---|
| 캐릭터 스프라이트 | 자체 생성 (코드) |
| 홀드 · 벽 · 매트 · 벤치 | 자체 생성 (코드) |
| UI 그래픽 | 자체 생성 (CSS) |
| 아이콘 | 시스템 이모지 (OS 제공, 번들하지 않음) |
| 폰트 | 시스템 폰트 (번들하지 않음) |

외부 이미지·유료 에셋·저작권 불명 리소스: **없음**
